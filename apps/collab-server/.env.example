# Same Postgres as the web app (Neon in prod).
DATABASE_URL="postgresql://USER@localhost:5432/localfirstdocs?schema=public"
PORT=1234
# Shared with the Next app; used to verify the WS handshake token.
COLLAB_TOKEN_SECRET="replace-me"
